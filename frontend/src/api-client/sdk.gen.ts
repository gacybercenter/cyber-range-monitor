// This file is auto-generated by @hey-api/openapi-ts

import { type Options as ClientOptions, type TDataShape, type Client, urlSearchParamsBodySerializer } from '@hey-api/client-axios';
import type { LoginData, LoginError, LogoutData, CurrentUserData, CurrentUserResponse, GetAllUsersData, GetAllUsersResponse, CreateUserData, CreateUserResponse, CreateUserError, AllUserDetailsData, AllUserDetailsResponse, UserDetailsData, UserDetailsResponse2, UserDetailsError, DeleteUserData, DeleteUserResponse, DeleteUserError, ReadUserData, ReadUserResponse, ReadUserError, UpdateUserData, UpdateUserResponse, UpdateUserError, SummaryData, SummaryResponse, SearchData, SearchResponse, SearchError, LogsFromTodayData, LogsFromTodayResponse, LogsFromTodayError, ReadAllOpenstackSourcesData, ReadAllOpenstackSourcesResponse, CreateOpenstackDatasourceData, CreateOpenstackDatasourceResponse, CreateOpenstackDatasourceError, ReadOpenstackSourceDetailsData, ReadOpenstackSourceDetailsResponse, ReadOpenstackSourceDetailsError, ToggleOpenstackSourceData, ToggleOpenstackSourceResponse, ToggleOpenstackSourceError, EnabledOpenstackConnectionStatusData, EnabledOpenstackConnectionStatusResponse, TestOpenstackConnectionData, TestOpenstackConnectionResponse, TestOpenstackConnectionError, DeleteOpenstackSourceData, DeleteOpenstackSourceResponse, DeleteOpenstackSourceError, ReadOpenstackSourceData, ReadOpenstackSourceResponse, ReadOpenstackSourceError, UpdateOpenstackSourceData, UpdateOpenstackSourceResponse, UpdateOpenstackSourceError, ReadAllGuacamoleSourcesData, ReadAllGuacamoleSourcesResponse, CreateGuacamoleSourceData, CreateGuacamoleSourceResponse, CreateGuacamoleSourceError, ReadGuacamoleDetailsData, ReadGuacamoleDetailsResponse, ReadGuacamoleDetailsError, ToggleGuacamoleSourceData, ToggleGuacamoleSourceResponse, ToggleGuacamoleSourceError, GuacamoleConnectionStatusData, GuacamoleConnectionStatusResponse, TestGuacamoleConnectionData, TestGuacamoleConnectionResponse, TestGuacamoleConnectionError, DeleteGuacamoleSourceData, DeleteGuacamoleSourceResponse, DeleteGuacamoleSourceError, ReadGuacamoleSourceData, ReadGuacamoleSourceResponse, ReadGuacamoleSourceError, UpdateGuacamoleSourceData, UpdateGuacamoleSourceResponse, UpdateGuacamoleSourceError } from './types.gen';
import { client as _heyApiClient } from './client.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Login
 * Checks the credentials provided by the 'AuthForm'
 * and in the response sets a cookie with the session id when given
 * valid login credentials.
 *
 * Arguments:
 * auth_form {AuthForm} -- Has types to enforce constraints on the request body
 * client {RequireClientIdentity} -- The client identity
 * user_service {UserController} -- The user controller
 * Raises:
 * HTTPUnauthorized: 401 - If the user is not authenticated
 * Returns:
 * Response
 */
export const login = <ThrowOnError extends boolean = false>(options: Options<LoginData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<unknown, LoginError, ThrowOnError>({
        ...urlSearchParamsBodySerializer,
        url: '/auth/login/',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options?.headers
        }
    });
};

/**
 * Logout
 * Logs out the user using the _"api_key" dependency_
 *
 * - Load the clients signed api key from the clients cookies
 * - Decrypts the api key in the Redis store,
 *
 * - if the key hasn't been tampered with by the user and is valid the
 * session is revoked by deleting the key in the Redis store mapped to the api key
 * and the cookie is removed from the client
 * Arguments:
 * request {Request}  - the request to get the existing session ID from
 * Raises:
 * If the api_key is not found or was tampered with
 * by the client
 * Returns:
 * JSONResponse -- a message indicating the logout was successful
 */
export const logout = <ThrowOnError extends boolean = false>(options?: Options<LogoutData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).post<unknown, unknown, ThrowOnError>({
        url: '/auth/logout/',
        ...options
    });
};

/**
 * Current User
 * Reads the current user
 *
 * Arguments:
 * reader {CurrentUser} -- the reader
 *
 * Returns:
 * UserResponse -- the current user
 */
export const currentUser = <ThrowOnError extends boolean = false>(options?: Options<CurrentUserData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<CurrentUserResponse, unknown, ThrowOnError>({
        url: '/users/me/',
        ...options
    });
};

/**
 * Get All Users
 * reads all users based on the role of the reader (no read up)
 * Arguments:
 * user_service {UserController} -- _the user controller_
 * reader {CurrentUser} -- _the reader_
 *
 * Returns:
 * list[UserResponse] -- _description_
 */
export const getAllUsers = <ThrowOnError extends boolean = false>(options?: Options<GetAllUsersData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<GetAllUsersResponse, unknown, ThrowOnError>({
        url: '/users/',
        ...options
    });
};

/**
 * Create User
 * **[ADMIN]**
 * Creates a user given valid form data and inserts
 * the created user into the database and returns the
 * created user
 *
 * _Arguments_:
 * - create_schema {CreateUserForm} -- the form data
 * - db {AsyncSession} - the database session
 * _Raises_:
 * HTTPException: 400 - Username is already taken
 * _Returns_:
 * UserResponse -- the created user
 */
export const createUser = <ThrowOnError extends boolean = false>(options: Options<CreateUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateUserResponse, CreateUserError, ThrowOnError>({
        ...urlSearchParamsBodySerializer,
        url: '/users/',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options?.headers
        }
    });
};

/**
 * All User Details
 * Admin protected route to read all of the user details, including
 * the creation date and last updated timestamps
 *
 * Arguments:
 * db {requires_db}
 *
 * Returns:
 * list[UserDetailsResponse]
 */
export const allUserDetails = <ThrowOnError extends boolean = false>(options?: Options<AllUserDetailsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<AllUserDetailsResponse, unknown, ThrowOnError>({
        url: '/users/details',
        ...options
    });
};

/**
 * User Details
 * reads the details of an individual user
 *
 * Arguments:
 * user_id {int} -- the id of the user to read
 * db {DatabaseRequired} -- the database session
 *
 * Raises:
 * HTTPNotFound: the user does not exist
 *
 * Returns:
 * UserDetailsResponse -- the details of the individual user
 */
export const userDetails = <ThrowOnError extends boolean = false>(options: Options<UserDetailsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<UserDetailsResponse2, UserDetailsError, ThrowOnError>({
        url: '/users/details/{user_id}/',
        ...options
    });
};

/**
 * Delete User
 * Deletes a user given an existing user ID
 *
 * Arguments:
 * user_id {int} -- the ID of the user to be deleted
 * db {requires_db} -- a database session
 * admin {admin_required} -- an admin user, ensures the Admin isn't deleting
 * themselves
 * Returns:
 * ResponseMessage -- A message indicating the deletion was successful
 */
export const deleteUser = <ThrowOnError extends boolean = false>(options: Options<DeleteUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteUserResponse, DeleteUserError, ThrowOnError>({
        url: '/users/{user_id}/',
        ...options
    });
};

/**
 * Read User
 * Reads a user with 'no read up' i.e a user cannot read a user with a
 * higher role / permission
 *
 * Arguments:
 * user_id {int} -- the id of the user to read
 * db {requires_db} -- the database session
 * reader {role_required} -- the "reader" or user making the request
 *
 * Raises:
 * HTTPNotFound: The user does not exist
 * HTTPForbidden: The user does not have permission to read the user
 *
 * Returns:
 * UserResponse -- The user attempting to be read
 */
export const readUser = <ThrowOnError extends boolean = false>(options: Options<ReadUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ReadUserResponse, ReadUserError, ThrowOnError>({
        url: '/users/{user_id}/',
        ...options
    });
};

/**
 * Update User
 * updates the user given an id and uses the schema to update the user's data
 *
 * Arguments:
 * user_id {int} -- user id of the user to update
 * update_schema {UpdateUser} -- the request body schema
 * db {AsyncSession} -- the database session
 * Returns:
 * UserResponse -- the updated user
 */
export const updateUser = <ThrowOnError extends boolean = false>(options: Options<UpdateUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateUserResponse, UpdateUserError, ThrowOnError>({
        ...urlSearchParamsBodySerializer,
        url: '/users/{user_id}/',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options?.headers
        }
    });
};

/**
 * Summary
 * returns a summary of recent event logs
 * Arguments:
 * log_service {LogServiceDep}
 * Returns:
 * LogMetaData
 */
export const summary = <ThrowOnError extends boolean = false>(options?: Options<SummaryData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<SummaryResponse, unknown, ThrowOnError>({
        url: '/logs/summary/',
        ...options
    });
};

/**
 * Search
 * Searches the logs based on the query parameters
 * Arguments:
 * query_params {Annotated[LogQueryParams, Query} -- the query params
 * log_service {LogServiceDep} -- the log service dep
 * Raises:
 * HTTPNotFound: if no logs are found matching the query parameters
 * Returns:
 * LogQueryResponse -- the logs that match the query parameters
 */
export const search = <ThrowOnError extends boolean = false>(options?: Options<SearchData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<SearchResponse, SearchError, ThrowOnError>({
        url: '/logs/search/',
        ...options
    });
};

/**
 * Logs From Today
 * Given a query filter, returns the logs from today that match the filter
 *
 * Arguments:
 * query_filter {Annotated[LogQueryParams, Query} the query params
 * log_service {LogServiceDep} -- the log service dep
 *
 * Raises:
 * HTTPNotFound: if no logs are found matching the query parameters
 *
 * Returns:
 * LogQueryResponse -- the logs that match the query parameters
 */
export const logsFromToday = <ThrowOnError extends boolean = false>(options?: Options<LogsFromTodayData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<LogsFromTodayResponse, LogsFromTodayError, ThrowOnError>({
        url: '/logs/today/',
        ...options
    });
};

/**
 * Read All Openstack Sources
 */
export const readAllOpenstackSources = <ThrowOnError extends boolean = false>(options?: Options<ReadAllOpenstackSourcesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ReadAllOpenstackSourcesResponse, unknown, ThrowOnError>({
        url: '/datasources/openstack/',
        ...options
    });
};

/**
 * Create Openstack Datasource
 * Creates a new openstack datasource
 *
 * Arguments:
 * openstack_data {Annotated[OpenstackCreateForm, Form} -- the openstack datasource data
 * openstack_controller {OpenstackControllerDep} -- the openstack controller dependency
 *
 * Returns:
 * OpenstackRead -- the created openstack datasource
 */
export const createOpenstackDatasource = <ThrowOnError extends boolean = false>(options: Options<CreateOpenstackDatasourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateOpenstackDatasourceResponse, CreateOpenstackDatasourceError, ThrowOnError>({
        ...urlSearchParamsBodySerializer,
        url: '/datasources/openstack/',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options?.headers
        }
    });
};

/**
 * Read Openstack Source Details
 * Reads the details of an openstack datasource, (i.e w/ the password)
 *
 * Arguments:
 * source_id {PathID} -- the datasource id
 * openstack_controller {OpenstackControllerDep} -- the controller dependency
 *
 * Returns:
 * OpenstackProtectedRead -- the openstack datasource details
 */
export const readOpenstackSourceDetails = <ThrowOnError extends boolean = false>(options: Options<ReadOpenstackSourceDetailsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ReadOpenstackSourceDetailsResponse, ReadOpenstackSourceDetailsError, ThrowOnError>({
        url: '/datasources/openstack/{source_id}/protected',
        ...options
    });
};

/**
 * Toggle Openstack Source
 */
export const toggleOpenstackSource = <ThrowOnError extends boolean = false>(options: Options<ToggleOpenstackSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ToggleOpenstackSourceResponse, ToggleOpenstackSourceError, ThrowOnError>({
        url: '/datasources/openstack/toggle/{source_id}',
        ...options
    });
};

/**
 * Enabled Openstack Connection Status
 * Attempts to connect to the openstack datasource returning
 * the result of the connection attept
 *
 * Arguments:
 * openstack_controller {OpenstackControllerDep} -- the controller dependency
 *
 * Returns:
 * GenericAPIResponse -- the api response with the details on the connection attempt
 * and errors, if Any
 */
export const enabledOpenstackConnectionStatus = <ThrowOnError extends boolean = false>(options?: Options<EnabledOpenstackConnectionStatusData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<EnabledOpenstackConnectionStatusResponse, unknown, ThrowOnError>({
        url: '/datasources/openstack/connection/status',
        ...options
    });
};

/**
 * Test Openstack Connection
 * Tests the connection to the openstack datasource by ID
 *
 * Arguments:
 * source_id {PathID} -- the ID of the datasource
 * openstack_controller {OpenstackControllerDep} -- the controller dependency
 *
 * Returns:
 * GenericAPIResponse -- the api response with the details on the connection attempt
 */
export const testOpenstackConnection = <ThrowOnError extends boolean = false>(options: Options<TestOpenstackConnectionData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<TestOpenstackConnectionResponse, TestOpenstackConnectionError, ThrowOnError>({
        url: '/datasources/openstack/connection/test/{source_id}',
        ...options
    });
};

/**
 * Delete Openstack Source
 */
export const deleteOpenstackSource = <ThrowOnError extends boolean = false>(options: Options<DeleteOpenstackSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteOpenstackSourceResponse, DeleteOpenstackSourceError, ThrowOnError>({
        url: '/datasources/openstack/{source_id}',
        ...options
    });
};

/**
 * Read Openstack Source
 * Reads an openstack datasource by ID
 *
 * Arguments:
 * source_id {PathID} -- The ID of the datasource
 * openstack_controller {OpenstackControllerDep} -- the controller dependency
 *
 * Returns:
 * OpenstackRead -- the openstack datasource
 */
export const readOpenstackSource = <ThrowOnError extends boolean = false>(options: Options<ReadOpenstackSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ReadOpenstackSourceResponse, ReadOpenstackSourceError, ThrowOnError>({
        url: '/datasources/openstack/{source_id}',
        ...options
    });
};

/**
 * Update Openstack Source
 * Updates an openstack datasource by ID
 *
 * Arguments:
 * source_id {PathID} -- The ID of the datasource
 * openstack_data {Annotated[OpenstackCreateForm, Form} -- the updated data
 * openstack_controller {OpenstackControllerDep} -- the controller dependency
 *
 * Returns:
 * OpenstackRead -- the resulting updated datasource
 */
export const updateOpenstackSource = <ThrowOnError extends boolean = false>(options: Options<UpdateOpenstackSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateOpenstackSourceResponse, UpdateOpenstackSourceError, ThrowOnError>({
        ...urlSearchParamsBodySerializer,
        url: '/datasources/openstack/{source_id}',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options?.headers
        }
    });
};

/**
 * Read All Guacamole Sources
 * returns a list of all the Guacamole datasources in the system
 * Arguments:
 * guac_controller {GuacControllerDep} -- the controller
 *
 * Returns:
 * GuacamoleListResponse -- the response model
 */
export const readAllGuacamoleSources = <ThrowOnError extends boolean = false>(options?: Options<ReadAllGuacamoleSourcesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ReadAllGuacamoleSourcesResponse, unknown, ThrowOnError>({
        url: '/datasources/guacamole/',
        ...options
    });
};

/**
 * Create Guacamole Source
 * Given a form with the data to create a Guacamole datasource and an admin
 * user with the proper permissions, creates a new Guacamole datasource.
 *
 * Arguments:
 * guac_create_data {Annotated[GuacamoleCreateForm, Form} -- the form with the data to create a Guacamole datasource
 * guac_controller {GuacControllerDep} -- the controller to use to create the Guacamole datasource
 *
 * Returns:
 * GuacamoleRead -- the resulting the datasource
 */
export const createGuacamoleSource = <ThrowOnError extends boolean = false>(options: Options<CreateGuacamoleSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateGuacamoleSourceResponse, CreateGuacamoleSourceError, ThrowOnError>({
        ...urlSearchParamsBodySerializer,
        url: '/datasources/guacamole/',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options?.headers
        }
    });
};

/**
 * Read Guacamole Details
 * allows the reader to see the password field for the datasource
 *
 * Arguments:
 * source_id {PathID} -- the ID of the datasource to read
 * guac_controller {GuacControllerDep} -- the controller dependency
 *
 * Returns:
 * GuacamoleProtectedRead -- the complete datasource model
 */
export const readGuacamoleDetails = <ThrowOnError extends boolean = false>(options: Options<ReadGuacamoleDetailsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ReadGuacamoleDetailsResponse, ReadGuacamoleDetailsError, ThrowOnError>({
        url: '/datasources/guacamole/details/{source_id}',
        ...options
    });
};

/**
 * Toggle Guacamole Source
 * given an ID of a Guacamole datasource, toggles the enabled datasource if possible
 *
 * Arguments:
 * source_id {int} -- ID of the datasource to toggle
 * guac_controller {GuacControllerDep} -- the controller dependency
 *
 * Returns:
 * GuacamoleRead -- the resulting datasource model
 */
export const toggleGuacamoleSource = <ThrowOnError extends boolean = false>(options: Options<ToggleGuacamoleSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ToggleGuacamoleSourceResponse, ToggleGuacamoleSourceError, ThrowOnError>({
        url: '/datasources/guacamole/toggle/{source_id}/',
        ...options
    });
};

/**
 * Guacamole Connection Status
 * tests the connection to the Guacamole datasource and returns a message
 * to the user if the connection was successful or not
 *
 * Arguments:
 * guac_controller {GuacControllerDep} -- the controller dependency
 *
 * Returns:
 * GenericAPIResponse -- the API response model with a message and the result of the connection test
 */
export const guacamoleConnectionStatus = <ThrowOnError extends boolean = false>(options?: Options<GuacamoleConnectionStatusData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<GuacamoleConnectionStatusResponse, unknown, ThrowOnError>({
        url: '/datasources/guacamole/connection',
        ...options
    });
};

/**
 * Test Guacamole Connection
 * attempts to create a session to the Guacamole datasource and whether the
 * connection was successful or not, returns a message to the user
 * Arguments:
 * source_id {PathID} -- the ID of the Guacamole datasource to test
 * guac_controller {GuacControllerDep} -- the controller dependency
 * Returns:
 * GenericAPIResponse -- the API response model with a message and the result of the connection test
 */
export const testGuacamoleConnection = <ThrowOnError extends boolean = false>(options: Options<TestGuacamoleConnectionData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<TestGuacamoleConnectionResponse, TestGuacamoleConnectionError, ThrowOnError>({
        url: '/datasources/guacamole/connection/test/{source_id}',
        ...options
    });
};

/**
 * Delete Guacamole Source
 * deletes a Guacamole datasource by its ID
 *
 * Arguments:
 * source_id {PathID} -- the ID of the datasource to delete
 * guac_controller {GuacControllerDep} -- the controller dependency
 *
 * Returns:
 * GenericAPIResponse -- the API response model with a message and the ID of the deleted datasource
 */
export const deleteGuacamoleSource = <ThrowOnError extends boolean = false>(options: Options<DeleteGuacamoleSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteGuacamoleSourceResponse, DeleteGuacamoleSourceError, ThrowOnError>({
        url: '/datasources/guacamole/{source_id}/',
        ...options
    });
};

/**
 * Read Guacamole Source
 * returns the Guacamole datasource model by its ID, excluding the password field
 *
 * Arguments:
 * source_id {PathID} -- the ID of the datasource to read
 * guac_controller {GuacControllerDep} -- the controller dependency
 *
 * Returns:
 * GuacamoleRead -- the resulting datasource model
 */
export const readGuacamoleSource = <ThrowOnError extends boolean = false>(options: Options<ReadGuacamoleSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ReadGuacamoleSourceResponse, ReadGuacamoleSourceError, ThrowOnError>({
        url: '/datasources/guacamole/{source_id}/',
        ...options
    });
};

/**
 * Update Guacamole Source
 * updates a Guacamole datasource by its ID
 *
 * Arguments:
 * source_id {PathID} -- the ID of the datasource to update
 * guac_update_data {Annotated[GuacamoleUpdateForm, Form} -- the form with the data to update the Guacamole
 * datasource.
 * guac_controller {GuacControllerDep} -- the controller dependency
 *
 * Returns:
 * GuacamoleRead -- the model updated
 */
export const updateGuacamoleSource = <ThrowOnError extends boolean = false>(options: Options<UpdateGuacamoleSourceData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateGuacamoleSourceResponse, UpdateGuacamoleSourceError, ThrowOnError>({
        ...urlSearchParamsBodySerializer,
        url: '/datasources/guacamole/{source_id}/',
        ...options,
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
            ...options?.headers
        }
    });
};