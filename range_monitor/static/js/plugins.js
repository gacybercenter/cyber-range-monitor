const main = document.querySelector("main");
document.addEventListener("DOMContentLoaded", () => {
    main.classList.add("star-background");  
    // only have 2 do this bc I didn't want to set in base template the background
    // for all 2 be stars
});